type Query {
  teachers: [Teacher!]!
}

type Mutation {
  createTeacher(name: String!, disciplines: String): Teacher
  deleteTeacher(id: ID!): Teacher
  updateTeacher(id: ID!, name: String, disciplines: String): Teacher
}

type Teacher {
  id: ID!
  name: String!
  disciplines: String!
}
